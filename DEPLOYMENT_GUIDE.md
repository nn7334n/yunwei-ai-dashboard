# 云微传媒 AI 控制面板 - 双平台部署教程与排坑实战指南
> **适用受众**：技术实施工程师、系统运维人员、独立部署客户  
> **适用平台**：macOS (Apple Silicon / Intel) & Windows 10/11 (64位)  
> **更新时间**：2026年3月

---

## 1. 部署前准备清单

在部署本系统前，请确保目标机器具备以下基础运行环境：

| 依赖项 | 最低版本要求 | 官方获取渠道 / 说明 |
| :--- | :--- | :--- |
| **操作系统** | macOS 12+ 或 Windows 10/11 64位 | 建议系统预留至少 20GB 以上磁盘空间用于存放模型 |
| **Python** | Python 3.9 ~ 3.12 | [Python 官网下载](https://www.python.org/downloads/)（Windows 安装时务必勾选 `Add python.exe to PATH`） |
| **Ollama 引擎** | 最新正式版 | [Ollama 官网下载](https://ollama.com/download)（双击安装即可） |

> [!NOTE]
> **本控制台设计原则**：控制面板服务端自身基于 Python 原生库编写，**不需要手动执行任何额外的 `pip install`** 即可直接启动！Open WebUI 所需的依赖环境由控制台在网页端一键自动化完成。

---

## 2. macOS 平台部署指南

### 第一步：安装推理引擎 Ollama
1. 访问 [ollama.com/download/mac](https://ollama.com/download/mac) 下载 `Ollama-darwin.zip`。
2. 解压并将 `Ollama.app` 拖入 `/Applications`（应用程序目录），首次打开点击“Install CLI tools”。

### 第二步：解压并进入项目目录
将包含 `ai-dashboard` 的工程文件夹放置在合适路径，打开终端（Terminal）执行：
```bash
cd "/Users/你的用户名/Documents/云微传媒_AI知识库 5/ai-dashboard"
```

### 第三步：一键赋予权限并拉起面板
```bash
# 赋予启动脚本执行权限
chmod +x start.sh

# 运行一键启动脚本
./start.sh
```
此时终端将输出：
```text
AI 控制面板已启动: http://localhost:9090
```
系统将自动为您唤起默认浏览器并打开控制台。

### 第四步：控制台内一键部署 Open WebUI
1. 在打开的 `http://localhost:9090` 页面中，观察中部的 **Open WebUI 界面** 卡片。
2. 此时状态指示灯为黄色 `未安装`，点击按钮 **“一键部署”**。
3. 在弹出的全自动部署窗口中点击 **“开始一键部署”**。
4. 控制面板将在后台自动为您执行：
   - 校验 Python 环境；
   - 在用户主目录下建立隔离虚拟环境 `~/open-webui-venv`；
   - 配置清华大学/阿里云高速 PyPI 镜像源；
   - 静默下载并编译 Open WebUI。
5. 部署进度条跑满并显示“部署圆满成功”后，关闭弹窗，卡片按钮将自动变为绿色 **“启动”**。

### 第五步：一键装机全家桶（按算力智能推荐）
在页面顶部的 **“智能硬件算力评测”** 横幅中，系统已识别您的硬件规格（例如：*32GB 专业旗舰配置*）。  
点击右侧 **“⚡ 一键部署黄金推荐全家桶”** 按钮，系统将自动依次在后台静默下载适配您硬件的最佳模型（如 `qwen2.5:14b` 与知识库向量底座 `bge-m3`），无需任何手动干预。

---

## 3. Windows 平台部署指南

### 第一步：安装 Python 与 Ollama
1. 安装 Python 3.10 或 3.11，**安装界面的第一个弹窗底部务必勾选：`Add python.exe to PATH`**。
2. 访问 [ollama.com/download/windows](https://ollama.com/download/windows) 下载并双击运行 `OllamaSetup.exe`，安装完成后屏幕右下角托盘会出现羊驼图标。

### 第二步：一键双击启动
1. 进入 `ai-dashboard` 目录。
2. 直接**鼠标双击运行 `start.bat`**。
3. 批处理脚本将自动完成：
   - 清洗可能造成网络阻塞的系统代理变量；
   - 检查并释放 9090 端口；
   - 以后台静默方式拉起 Python 控制面板服务；
   - 自动唤起 Edge/Chrome 浏览器访问 `http://localhost:9090`。

### 第三步：一键部署 WebUI 与模型
与 Mac 操作完全一致：
- 在网页中点击 Open WebUI 卡片上的 **“一键部署”**；
- 系统会自动在 `%USERPROFILE%\open-webui-venv` 下创建 Windows 专用虚拟环境并拉取依赖；
- 在顶部点击 **“⚡ 一键部署黄金推荐全家桶”** 完成模型安装。

---

## 4. 局域网移动端 / 多设备协作访问

控制台已经内置**智能物理网卡探测算法**，会自动过滤虚拟代理网卡（如 Clash/Surge 的 TUN 虚假网卡），并在顶部标题栏右侧展示专属绿色徽章：
```text
手机/局域网: http://192.168.31.108:9090 [点击复制]
```

### 多设备访问步骤：
1. 确保手机、iPad 或同办公室其他电脑连接到同一个 Wi-Fi（路由器）。
2. 在手机浏览器地址栏输入控制台给出的局域网地址（例如 `http://192.168.31.108:9090`）。
3. 手机端可直接管理模型、启动服务，或直接访问 `http://192.168.31.108:8080` 进行知识库智能问答。

---

## 5. 常见排坑与故障诊断 (FAQ)

### Q1：点击启动后浏览器显示“无法访问此网站”？
- **排查 1（端口冲突）**：检查是否有其他进程霸占了 9090 端口。
  - Mac 终端排查：`lsof -ti:9090 | xargs kill -9`
  - Windows CMD 排查：`netstat -ano | findstr :9090`
- **排查 2（后台进程未就绪）**：直接在终端前台运行排查报错信息：
  ```bash
  python3 server.py
  ```

### Q2：局域网内其他设备打不开控制台？
- **Windows 防火墙拦截**：
  Windows 在首次运行 Python 服务时会弹出“Windows 安全中心警报 - 是否允许 Python 访问专用网络”，**务必勾选“专用网络”并点击允许**。
  若已误关，请在“控制面板 -> Windows Defender 防火墙 -> 允许应用通过防火墙”中勾选 Python。
- **Wi-Fi 路由器开启了 AP 隔离**：
  某些企业级或访客 Wi-Fi 开启了“客户端隔离”（AP Isolation），导致同局域网设备间无法互访。连接常规家庭 Wi-Fi 或关闭隔离即可。

### Q3：下载模型时速度很慢或中断报错？
- 控制台在下载模型时直接与 Ollama Registry 官方通道通信。如果因跨境网络波动导致中断：
  1. 控制台顶部下载进度卡片会变红并提示 `重新尝试`，点击重试即可断点续传；
  2. 若有科学上网工具（如 Clash），请开启全局模式或 TUN 模式后再点击下载；
  3. 控制台在设计时已对代理网卡进行了智能分流，不受 TUN 虚拟网卡 IP 漂移影响。

### Q4：如何彻底释放电脑的所有显存与内存？
- 方式一：在控制台点击右上角快捷控制栏中的 **“全部关闭 (释放资源)”** 红色按钮。
- 方式二：在已安装模型列表中，针对当前运行的模型点击 **“关闭运行”**，即可仅卸载模型显存，保持服务进程常驻随时秒级唤醒。
